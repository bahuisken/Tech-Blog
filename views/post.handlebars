{{{get_borderBg}}}
<div class="container">
  <div class="row">
    <div class="font-weight-bold col-6">{{title}}</div>
    <div class="text-right col-6 font-weight-light text-monospace">Posted by {{user.name}} on {{format_date
      date_created}}</div>
  </div>
</div>
</div>
<div class="card-body text-dark">
  <p class="card-text">{{content}}</p>
</div>
</div>